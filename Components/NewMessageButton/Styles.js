import { StyleSheet} from 'react-native'

export const styles = StyleSheet.create({
    container: {
        position: 'absolute',
        bottom: 60,
        right: 30,
        backgroundColor: 'green',
        padding: 20,
        borderRadius: 50
    }
})
